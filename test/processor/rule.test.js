'use strict';
const assert = require('assert');
const { compile } = require('../../src/processor')

// Just for comparison
describe('compile', () => {
  it('needs to be written TODO', () => {
    assert.ok(true);
  });
});
